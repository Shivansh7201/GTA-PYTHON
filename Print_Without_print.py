"""
   Print without newline Using Python sys module
   To use the sys module, first, import the module sys using the import keyword. 
   Then, make use of the stdout.write() method available inside the sys module, 
   to print your strings. It only works with string If you pass a number or a list, 
   you will get a TypeError.
"""
import sys

sys.stdout.write("GeeksforGeeks ")
sys.stdout.write("is best website for coding!")
